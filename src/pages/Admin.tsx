
import React from 'react';

const Admin = () => (
  <div style={{ padding: '2rem' }}>
    <h1>🔐 Admin Panel</h1>
    <p>Manage users, tokens, and platform settings.</p>
  </div>
);

export default Admin;